/*
 * File:   SemanticAnalyzer.cpp
 * Author: juan.garibotti
 *
 * Created on 28 de marzo de 2014, 11:46
 */

#include "SemanticAnalyzer.hpp"

namespace cfg
{
    namespace sem
    {
//        Property AnalyzeProperty( cfg::Property const& i_property )
//        {
//
//
//            return i_property;
//        }

        SymbolTable::SymbolTable( ConfigAST const& i_ast )
        {
//            m_scopes.push_back( Scope() );
//            std::vector<Scope*> scopeStack ( m_scopes.back() );
//            for ( auto property ( i_ast.m_root.m_lines.begin()), end ( i_ast.m_root.m_lines.end() ); property != end; ++property )
//            {
//                if ( property->m_type == Assignment )
//                {
//                    scopeStack.back()->m_properties.push_back( AnalyzeProperty( *property ) );
//                }
//                else
//                {
//                    m_scopes.push_back( Scope() );
//                    m_scopes.back().m_name += scopeStack.back()
//                }
//            }
        }

    }
}

